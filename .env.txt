```env
# String de conexão para o PostgreSQL local
# Substitua SUA_SENHA_AQUI pela senha que você definiu no Passo 1.
galint_db="postgresql://postgres:3384@localhost:5432/galint_db"

# Token para o dashboard compartilhado (opcional, mas recomendado)
# Troque por qualquer palavra ou frase secreta.
3384="seu-token-secreto-aqui"

# Caminhos para os utilitários do PostgreSQL (necessário para backup/restore)
# Verifique se os caminhos correspondem à sua instalação.
GALINT_PG_DUMP="C:\\Program Files\\PostgreSQL\\16\\bin\\pg_dump.exe"
GALINT_PSQL="C:\\Program Files\\PostgreSQL\\16\\bin\\psql.exe"
```